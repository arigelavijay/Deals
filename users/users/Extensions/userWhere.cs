using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

using users.Models;

namespace users.Extensions
{
    public static class userWhere
    {
        /*
        public static Func<deal, int, bool> FuncDealsWhere = delegate(deal d, int locationId)
        {
            return (d.locationId == locationId &&
                   d.isActive == true &&
                   d.count > d.sold &&
                   d.startsOn <= DateTime.Now &&
                   d.endsOn >= DateTime.Now);
        };
        
        public static delegate bool FuncDealsWhere(deal d, int locationId);

        public static bool MethodFuncDealsWhere(deal d, int locationId)
        {
            return (d.locationId == locationId &&
                   d.isActive == true &&
                   d.count > d.sold &&
                   d.startsOn <= DateTime.Now &&
                   d.endsOn >= DateTime.Now);
        }
        */
    }
}